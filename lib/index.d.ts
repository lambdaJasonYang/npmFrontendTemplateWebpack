import { d } from "./gg";
declare const dd: bleh;
declare const sayhello: (mystr: String) => string;
interface bobby {
    mode: string;
}
declare class bob implements bobby {
    mode: string;
    constructor(x: string);
}
export { sayhello, d, dd, bob };
